# 本机 npm 10.9.2 在解析 vitest 5 的超大可选 peer 集合时会在 arborist 的
# #loadPeerSet 中崩溃（TypeError: Cannot read properties of null (reading 'edgesOut')）。
# 关键 peer 约束已手工核对（vite 7 与各插件兼容，见 package.json），故跳过自动 peer 解析。
legacy-peer-deps=true
