# 里程碑、风险与决策记录

> 本文档是《开发文档》的 §11–§13 与附录 A 部分；全部文档见 [docs/README.md](README.md)。

## 11. 里程碑排期

单人全职估算。

| 阶段 | 内容 | 工期 | 验收标准 |
|---|---|---|---|
| **M0 · 骨架 + 技术验证** ✅ **已完成 2026-09-25** | electron-vite + React + TS + Tailwind 跑通；三进程 IPC 打通；`node:sqlite` 读写；**主进程成功抓取 NBS 并入库**（打通端到端采集链路） | 3-4 天 | ✅ 能开窗、能读 DB、能通过 IPC 拿到数据；NBS **10 个指标中 8 个**端到端跑通（20 条观测入库），2 个为已知缺口 |
| **M1a · 迁移 + 翻页 + 补齐两个缺口** ✅ **已完成 2026-09-25** | 真迁移机制（§3.3.1）；发布列表翻页回溯；GDP 表格解析（表级判别 + 季度行标签）；调查失业率通稿解析；1—2 月合并发布；口径守卫重构 | 2 天 | ✅ **NBS 的 10 个 P0 指标 10/10 全部接通**（22 条观测入库，0 失败）；离线 69 例 + 联网 9 例全绿；真机验证 M0 老库平滑迁移 v1 → v2；连跑两次刷新行数零增长 |
| **M1c · 移除采集层，改用示例种子** ✅ **已完成 2026-09-25** | 删除 `src/main/sources/`、`collector.ts` 及其联网测试；新增 `db/seed.ts` 确定性种子（5 年 / 535 条观测 / 五种 status 全覆盖）；`Indicator` 剥掉全部 `sourceXxx` 字段；`IPC.refresh` 语义改为「重置示例数据」；界面加常驻「示例数据，非真实统计」横幅 | 1 天 | ✅ 不再是「UI 等数据」而是「数据等 UI」；`npm test` 27 例全绿且全离线；真机验证：M0 老库（22 条真实观测）被示例数据替换、`seed_version` 落库、重启不重灌 |
| **M1d · 删掉生成器，数据改为内置文件** ✅ **已完成 2026-09-25** | 删除 `db/seed.ts` + `seed.test.ts` 及一次性生成脚本；数据固化为随项目提交的 `resources/macro.db`（135 KB / 535 条）；新增 `db/bootstrap.ts`（首次复制 / 重置覆盖）；`initDatabase()` 不再生成数据；新增 `electron-builder.yml`（`extraResources` 带上该文件）；测试从「测生成器」改为「测产物」；清理 M1c 残留的无用依赖 `cheerio` / `zod`（`dependencies` 随之清空） | 半天 | ✅ 应用**不生成任何数据**；`npm test` 25 例全绿且全离线；真机验证两条路径：首次运行复制（日志 `首次运行：内置示例数据已复制到 …`）、再次运行不覆盖（`使用已有数据库 …（未覆盖）`，文件 mtime 不变）；界面渲染正常（横幅 / 535 条 / 「1—2月合并」标签） |
| ~~**M1b · 首次全量初始化**~~ **暂缓** | 分级加载、可中断续跑、`release_index` 落库、进度 UI、GDP 矩阵历史加载器 | — | **暂缓原因：不做采集就不需要初始化**（§3.2.0） |
| ~~**M1.5 · 铺开其余源**~~ **暂缓** | 人民银行 → 海关 → 外汇局 → 财政部 | — | **暂缓原因同上。但重新接入时仍应优先做人民银行**，理由见下 |
| **M2a · 详情页折线图** ✅ **已完成 2026-09-25** | 引入 ECharts 6（按需引入，决策 12）；`charts/theme.ts`（色值从 CSS 变量读）；`charts/lineOption.ts`（纯函数构造 option）；`charts/LineChart.tsx`（实例生命周期）；详情页默认视图从数据表改为折线图，表格降级为「数据表」切换视图（§6.5 的硬要求） | 1 天 | ✅ `npm test` 42 例全绿；渲染进程 chunk 731,763 字节（按需比全量省 636,033 字节）；**用真库数据 SSR 渲染验证**：`cn.re.cum_yoy` 55 行含 1 个 null → 画 54 个标记、折线断成 2 段；深色模式实测网格线由 `--c-grid` 的 `#e1e0d9` 变为 `#2c2c2a`、序列色由 `--c-accent` 的 `#2a78d6` 变为 `#3987e5`，证明图表确实在读 CSS 令牌 |
| **M2b · 设计系统 + 概览** | 统计卡片、KPI 行、4 张核心图、概览页 | 3-4 天 | 概览页可用；深浅色均通过校验 |
| **M3 · 指标库 + 详情 + 对比** | 分类树、指标列表、详情页、口径切换、数据表、对比页 | 5-6 天 | 主流程闭环：浏览 → 详情 → 切换口径 → 对比 |
| **M4 · 日历 + 导出** | 发布日历、CSV/PNG 导出、数据管理页 | 3-4 天 | 能导出可用的 CSV；日历能反映发布状态 |
| **M5 · 打磨** | 空/错/加载状态、性能优化、无障碍、设置页、日志 | 4-5 天 | 键盘可操作全部功能；冷启动 < 2s；示例数据横幅在所有页面可见 |
| **M6 · 本地打包** | electron-builder `--dir` 生成未签名 `.app`、图标、放入 /Applications | 1-2 天 | 生成可双击启动的本地 `.app`（不签名、不公证、不自动更新）；**双击启动后能读到 535 条观测**（验证 `extraResources` 没漏配，见风险 14） |

**工期合计（只算表里还留着数字的行）：23.5–29.5 个工作日**，约 4.7–5.9 周。
其中 **M0–M2a 已完成，占 7.5–8.5 天**；**剩余 M2b–M6 是 16–21 天**，约 3.2–4.2 周。

> M1b / M1.5 已暂缓，工期格是「—」，所以它们**没有计入**上面的合计——本表推不出
> 接回真实数据源时要还多少工时，届时得重新估。
> （此处原先写「总计 29-39 个工作日 / 暂缓后 4.5-5.5 周」，两个数都对不上表里任何一组相加。）

> M0 已完成的部分（Schema、db adapter、`http.ts` 限速与退避、fixtures 测试框架、8 个指标的采集器）已从 M1 中扣除——其中采集部分随后在 M1c 又整体移除了。
>
> **M1 拆成 M1a/M1b 的原因：** 侦察后发现原 M1 是一整块 5-7 天的工作，且内部依赖是单向的——补齐缺口（M1a）是达成「P0 指标全部接通」的充分条件，而回填引擎与进度 UI（M1b）只影响**首次启动的体感**，不影响数据正确性。拆开后先把「数据层可信」这件事锁死并验证，再单独做体验优化。
>
> **M1c 改变的是里程碑顺序，不是终局。** 原计划是「数据层全部铺完（M1 + M1.5）再动 UI」，理由是怕口径未稳定导致图表返工。M1c 把这条反过来了：**先做 UI，数据用示例种子顶着**。根据是 M1a 结束时 10/10 已经接通、口径形态也已经实测清楚——口径的稳定性靠的是 `note` 与 `valueType` 这些**元数据**，而它们与数据是真是假无关。种子保证了格式与形态与真实一致，所以照着它做的图表在接真数据时不该返工；如果返工了，说明种子哪里没对齐真实格式，那是种子的 bug。
>
> ⚠️ **重新接入真实数据源时，顺序仍是「纵向打通 → 横向铺开」，且人民银行优先于其余源。** 见 §2 的说明——缺了 M2 与社融的宏观看板不具备可用性。届时 `b646f54` 的 `http.ts` / `nbs.ts` 与那批实测结论可直接复用（原文见 `git log -- docs/开发文档.md`）。
>
> **M1d 把「数据形状」这件事彻底冻结了。** 接真实数据源时，**读库那条路径**（`queries.ts`
> 的 `getSeries`）不用动——`bootstrap.test.ts` 里那组「经查询层读」的断言，届时可以直接
> 改成对着真实数据再跑一遍。**写库那条路径要新写**：原有的 `upsertObservations` /
> `logFetch` 在采集层删除后就再没有调用方，已作为死代码清掉（§3.2.0），`queries.ts` 现在
> 只剩读路径。真正要写的就两件：「从网上取数并转成行」和「把行写进 `observation`」。

---

## 12. 风险登记册

| # | 风险 | 影响 | 概率 | 对策 |
|---|---|---|---|---|
| 0 | **合成数据被当成真实统计**（M1c 新增，取代原 1/4 两条的位置） | **严重**——截图、演示、引用出去就是错误信息 | 中 | 四道防线见 §3.2.0：常驻不可关闭的横幅、源名不叫「国家统计局」、`fetch_log` 明写非真实、口径说明与数值分开陈述。⚠️ **四道里有三道渲染在界面上**——横幅（窗口顶部）、页脚的源名与 `fetch_log` 文案（`App.tsx` 的 `HealthDot`）、详情页常驻的口径说明。横幅是其中最醒目、也是唯一不可关闭的一道；删掉它，风险不会自动由另三道兜住 |
| 1 | ~~首次初始化失败或耗时过长~~ **暂缓**（M1c） | — | — | 当前不采集，因此不适用。重接时需要重新设计一套初始化流程 |
| 2 | ~~官方站点改版导致解析失败~~ **暂缓**（M1c） | — | — | 当前不采集，因此不适用。重接时各源解析器应做成可独立替换的模块 |
| 3 | **多源 HTML 解析维护成本高**（放弃 Python 的直接代价） | 长期维护负担 | — | **这正是 M1c 移除采集层的直接原因**（决策 10）。它不是被解决了，是被推迟了：重接时要一次性面对 |
| 4 | ~~采集页面改版后静默返回错误数字~~ **暂缓**（M1c） | — | — | 当前不采集，因此不适用。重接时必须沿用 §10.3 的「成对断言」纪律 |
| 5 | ~~用户端直连政府站点被限流 / 封 IP~~ **暂缓**（M1c） | — | — | 当前应用完全不联网，不存在被限流的可能 |
| 6 | 口径理解错误导致数据误导 | **严重** | 中 | 每个指标写 `note`；UI 固定展示口径与来源；M1 阶段人工比对官方发布稿 |
| 7 | 数据版权 / 合规 | 法律风险 | **低** | 库里是合成数据，不构成再分发；应用不对外分发（§1.4）。各指标的「口径说明」引用官方统计口径，合规说明见 §9.1 |
| 8 | **`node:sqlite` 标记为实验性 API，可能随 Node 版本变动** | 升级 Electron 后数据库层损坏 | 中 | 所有访问经 `db/adapter.ts` 单一封装，换实现是单文件改动；**升级 Electron 前必须跑通全量测试**；保留 `better-sqlite3` 作为备选 |
| 9 | ~~无网络环境首次启动无数据~~ **已消失**（M1d） | — | — | 数据随安装包提供，应用**完全不联网**，离线可用是默认状态而非降级状态 |
| 10 | 数据修订导致历史序列不一致 | 用户困惑 | 中 | 保留 `revision`；UI 标注"已修订" |
| 11 | ECharts 实例未销毁导致内存泄漏 | 长时间使用变卡 | 中 | ✅ **M2a 已落为代码**：`LineChart.tsx` 的 effect cleanup 里 `ro.disconnect(); chart.dispose(); chartRef.current = null`。React StrictMode 在 dev 下会 mount→unmount→mount，漏掉 `dispose()` 会直接撞上 ECharts 的「already initialized on the dom」——真机验证过没出现该警告。内存纳入后续验收 |
| 12 | 指标数量膨胀导致 UI 失控 | 体验下降 | 中 | 严格按 P0/P1/P2 分批推进；§2 的 P1/P2 是路线图，不提前实现 |
| 13 | **`resources/macro.db` 不可再生**（M1d 新增） | 想改一个示例值都做不到；文件一旦损坏或被误改，只能靠 `git` 回滚——而它是二进制，`git diff` 看不见改了什么 | **高**（是已接受的既定代价，不是意外） | ①已记录取回生成器的路径：`git checkout 3561b24 -- src/main/db/seed.ts`——⚠️ **但它取回来不能直接编译**，那个文件 import 的 `upsertObservations` / `logFetch` / `ObservationInput` 已作为死代码删除（§3.2.0），需先补回这三个符号；②测试从「测生成器」改为「测产物」，把界面依赖的每条性质钉住（§10.3）；③`.gitignore` 排除 `-wal`/`-shm`，避免过期边车文件污染主库 |
| 14 | **打包时漏配 `extraResources`**（M1d 新增） | 打出来的 `.app` 启动即失败——而 dev 下一切正常，属于「只在打包后才暴露」的缺陷 | 中 | `electron-builder.yml` 已配好并在此文档记录；`ensureUserDb` 抛的错里带完整路径与「检查 extraResources」的提示；M6 的验收标准含「双击启动的 `.app` 能读到 535 条观测」 |

---

## 13. 关键决策记录

以下架构决策已确认，作为后续开发与评审的约束基线：

| # | 决策 | 结论 | 日期 |
|---|---|---|---|
| 1 | **数据更新方式** | ⚠️ **原决策：纯运行时 Node 采集**。**已于决策 10 被取代**（§3.2） | 2026-09-25 |
| 2 | **目标平台** | ✅ **macOS 先行**（arm64）。代码与配置保持平台无关；Windows 构建推迟到 macOS 稳定之后 | 2026-09-25 |
| 3 | **MVP 数据源范围** | ⚠️ **原决策：NBS 单源纵向打通端到端**，随后按 人民银行 → 海关 → 外汇局 → 财政部 复制。**采集层已于决策 10 整体删除，本决策随之失效**；下面这条顺序如今只在「重新接入真实数据源」时才有意义（§11 M1a / M1.5） | 2026-09-25 |
| 4 | **SQLite 实现** | ✅ **`node:sqlite`（Node 内置）**。API 已实测验证（§4 备注）；经 `db/adapter.ts` 封装以隔离实验性 API 风险 | 2026-09-25 |
| 5 | **分发范围** | ✅ **不对外分发**——个人自用。不做签名、公证、自动更新、上架（§1.4） | 2026-09-25 |
| 6 | **NBS 取数路径** | ✅ **HTML 发布稿**（`www.stats.gov.cn/sj/zxfb/`）。原计划的 `easyquery.htm` JSON 接口被网宿 WAF 封禁（403 UrlACL），实测确认不可用，**不尝试绕过**。⚠️ 这条结论写在已删除的 §3.1 / §3.2.6 里，原文见 `git log -- docs/开发文档.md` | 2026-09-25 |
| 7 | **主进程模块格式** | ✅ **ESM**（`package.json` 设 `"type": "module"`）。实测 Electron 44 支持 ESM 主进程与 `import { app } from 'electron'` 具名导入；**preload 仍必须 CJS**（sandbox 限制） | 2026-09-25 |
| 8 | **源健康度状态** | ✅ 四态 **`ok / partial / fail / never`**。`partial` 不得折叠为 `fail`，否则会把「部分成功」显示成"采集失败"。四态枚举仍在 `types.ts`，`App.tsx` 仍照它渲染。⚠️ 原举的例子（「否则会把 8/10 正常的源显示成失败」「M1a 后 NBS 为 `ok`」）属采集时代——M1c 后只剩示例源 `mock` 一个，`fetch_log` 里就它一行 | 2026-09-25 |
| 9 | **迁移机制** | ✅ **`schema.sql` 冻结为 v1 基线，其后一切变更走 `db/migrate.ts`**。原实现对 `schema_version` 只写不读、每次启动压回 1，靠 `CREATE TABLE IF NOT EXISTS` 掩盖；第一次出现 `ALTER TABLE ADD COLUMN` 就会每次启动抛错（§3.3.1） | 2026-09-25 |
| 10 | **采集层去留（M1c）** | ✅ **整体删除网络采集，改为一次性静态示例种子**（§3.2.0）。理由：解析器要跟着官方改版长期维护，而当前阶段真正缺的是**界面**，不是数据。代价是库里变成合成数据——因此设了四道防线（常驻横幅 / 源名「示例数据」/ `fetch_log` 明写非真实 / 口径说明与数值分开陈述），并把「以 `app_meta.seed_version` 而非库是否为空」作为灌入判据，避免真数据被横幅说成假的。**代码与实测结论可从 `b646f54` 取回** | 2026-09-25 |
| 11 | **示例数据的形态（M1d）** | ✅ **连生成器一起删掉，数据固化为随项目提交的 `resources/macro.db`；应用启动时只把它复制到 userData，不生成任何数据**（§3.2.0）。理由：数据既然是一份固定的演示内容，就没有理由在运行时算出来——「应用在造数据」本身是多余的复杂度。**已明确知悉并接受的代价：该文件不可再生**（改不了、补不了、无法从参数重新生成），需要时得先 `git checkout 3561b24 -- src/main/db/seed.ts` 取回生成器；对冲手段是把测试从「测生成器」改为「测产物」。判据相应从 `app_meta.seed_version` 改为「userData 里文件是否存在」 | 2026-09-25 |
| 12 | **图表库与宿主组件（M2a）** | ✅ **ECharts 6，`echarts/core` 按需引入，自写 `charts/LineChart.tsx` 管实例生命周期；不装 `echarts-for-react`**（§4）。理由：该封装最新 3.0.6，peer 只声明到 `react >=16`，且它做不好我们要的三件事——主题跟随系统深浅色（要在 `prefers-color-scheme` 变化时重算 option，而不是当 prop 传一次）、`dispose()` 生命周期（§12 风险 11 点名的那条）、React 19 兼容性无上游保证。M1d 刚把 `dependencies` 清空，此时引入一个不维护的包装层不划算。**这是对 §4 原表的如实修订**，不是遗漏 | 2026-09-25 |
| 13 | **图表色值的唯一事实源** | ✅ **色值只在 `styles/index.css` 的 `--c-*` CSS 变量里定义，图表主题与 UI 共用同一套令牌；TS 侧不硬编码色值**（§6.7）。`charts/theme.ts` 只负责把 CSS 变量读出来，读不到时用一组兜底值。（本节原先记的是「以附录 C 的 `tokens` 对象为准、CSS 变量作兜底」——附录 C 已整节删除，它是 `theme.ts` 的一个虚构版本，留着就是第二份会各自漂移的事实源。）**实测收益**：深色模式下不改一行图表代码，网格线即由 `#e1e0d9` 变为 `#2c2c2a`、序列色由 `#2a78d6` 变为 `#3987e5` | 2026-09-25 |

**决策的连带影响**曾逐条同步进本文档的对应小节。这里原先维护着一份「决策 → 改了哪几节」的清单；那些小节如今已按现状重写完毕，清单本身只会成为第二份会漂移的记录，故去掉。

---

### 附录 A · 指标命名规范

```
cn.<主题>.<口径>           例如：cn.cpi.yoy

主题：gdp | ind_prod | cpi | ppi | pmi | retail | fai | re | trade
      m0 | m1 | m2 | tsf | loan | lpr | unemployment | fx_reserve | fx_rate | fiscal

口径：yoy        同比
      mom        环比
      cum_yoy    累计同比
      level      绝对值/余额
      mom_sa     环比（季调）
      index      指数
```
