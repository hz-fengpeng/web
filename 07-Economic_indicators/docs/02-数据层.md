# 数据层设计

> 本文档是《开发文档》的 §3 部分；全部文档见 [docs/README.md](README.md)。
> 源码注释以 `§3.2.0` / `§3.3` / `§3.4` 引用本文档。


## 3. 数据层设计

> 这一节决定项目的成败。UI 做得再漂亮，数据层不稳，应用就是个玩具。

### 3.1 数据源调研

> **本节已删除。** 原内容是重新接入真实数据源时的站点调研：国家数据的 `easyquery.htm`
> 接口被网宿 WAF 封禁（403 UrlACL，不绕过）、统计局发布库 `www.stats.gov.cn/sj/zxfb/`
> 是 NBS 的唯一可用路径、以及人民银行 / 海关总署 / 财政部 / 外汇局的接口形态与稳定性。
> 应用当前不联网，这些结论暂不适用。需要时见 `git log -- docs/开发文档.md`。

数据版权归原发布机构所有。

### 3.2 数据来源：内置示例数据库文件

> ⚠️ **库里的数值是合成的演示数据，不是真实统计。** 界面上有一条不可关闭的常驻横幅
> 说明这一点；各指标的「口径说明」记录的是**真实统计口径**，仅用于指导界面呈现。
>
> 数据是一份随项目提交的 SQLite 文件 `resources/macro.db`（135 KB，535 条观测）。
> 应用**不生成任何数据**，只负责把它摆到用户目录。为什么连生成器也一并删掉、
> 以及 M1c 移除网络采集层的经过，见 §13 决策 10 / 11。

#### 3.2.0 当前实现：内置数据库文件 + 首次运行复制

**数据从哪来**：`resources/macro.db`，随项目/安装包一起发布。应用不生成它，
启动时只把它复制到 `userData/macro.db`，之后一律读写 userData 那份。
逻辑全部在 `src/main/db/bootstrap.ts`（`ensureUserDb` / `resetToBundled`）。

**为什么是复制而不是直接打开内置文件**，两个理由缺一不可：

1. **迁移要写。** `migrate()` 要写 `app_meta.schema_version`，日后 schema 变更也要写。
   只读打开无处可写；可写打开则是往应用包内部写——打包后那是 `.app` 的一部分。
2. **用户数据不随安装包被覆盖。** 放 userData 是 Electron 的惯例。

**判据是「文件是否存在」，没有别的判据**：已存在就原样保留，绝不覆盖。
用户可能已经手改过库、或它已经从旧版本迁移过，那些都比刚复制进来的内置文件更权威。
（M1c 时代判据是 `app_meta.seed_version`，因为当时要处理「M0 抓来的真数据还在库里」
的问题；现在数据本身就是文件，文件在就说明之前已经摆过一次了。）

**路径怎么定**（`index.ts` 的 `dbPaths()`）：dev 用 `app.getAppPath()/resources/macro.db`；
打包后用 `process.resourcesPath/macro.db`——因为打包后主进程代码在 `app.asar` 里，
用 `app.getAppPath()` 拼 `resources/` 会指进 asar 内部。`electron-builder.yml` 的
`extraResources` 负责把文件放到那个位置。**这两处必须同时正确**，漏一处就是启动即失败。

**「重置示例数据」按钮的语义**：`IPC.refresh` → `resetToBundled()` → 覆盖 userData
那份回内置状态。用户若用 `sqlite3` 改坏了库，靠它回到出厂状态。

> ⚠️ **重置必须先关连接**，顺序错了会**静默失效**——不抛错、返回值正常，但一行都没换。
> 机制：`copyFileSync` 就地截断写、inode 不变，旧连接并不指向已消失的文件，它仍持有
> 自己的 WAL；覆盖之后 SQLite 把那份 WAL 重放回去，于是新连接读到的是「旧数据 +
> 旧连接的后续写入」，内置文件被整个盖掉。`bootstrap.test.ts` 里有一个**反例测试**
> 专门断言这个行为，把这个坑钉住。

**核心约束：库里是合成数据，必须让人一眼看出来。** 伪造的宏观统计数字看起来越像真的，
越容易被截图、演示、引用成真实数据——这是这类「占位数据」唯一真正的风险。因此：

| 防线 | 做法 |
|---|---|
| 界面 | 窗口顶部**常驻、不可关闭**的横幅：「示例数据，非真实统计」 |
| 数据源名 | `MOCK_SOURCE.nameZh = '示例数据'`，**刻意不叫「国家统计局」** |
| 采集日志 | `fetch_log.message = '内置示例数据文件 · 非真实统计数据'` |
| 口径说明 | `indicator.note` 记的是**真实统计口径**，与合成数值分开陈述 |

**文件里的数据长什么样**（生成规则见 `3561b24` 的 `src/main/db/seed.ts`）：

- **期间格式与真实一致**：月度 `YYYY-MM`、季度 `YYYYQn`，`period_end` 由 `periodEnd()` 统一算
  ——和原线上路径同一套代码，不是另写一份。
- **口径形态一致**：累计同比（社零/固投/房地产）是**年内累计均值**，每年 1 月重新起算；
  1—2 月合并的指标没有 1 月期间，2 月记 `status='merged'`。这样 UI 上「1—2月合并」
  那个标签才真的渲染得出来。
- **五种 status 全部出现**：`ok` / `merged` / `prelim` / `revised` / `missing` 各有样本，
  UI 不至于有分支从来没被走过。
- **发布日按真实节奏排**：PMI 当月最后一天、CPI/PPI 次月 9 日、其余月度指标次月 15 日、
  GDP 季后次月 16 日；且有测试断言发布日**不早于期末**。
- **覆盖范围**：5 年（60 个月 / 20 个季度），535 条观测。

**⚠️ 已知代价：这个文件不可再生。** 用户明确选择「彻底删掉生成器」，只提交二进制产物。
后果是 `resources/macro.db` 成了一个黑盒：改不了、补不了、也没法从参数重新生成一份。
真需要改数据时，路径是把生成器取回来：`git checkout 3561b24 -- src/main/db/seed.ts`。

> ⚠️ **取回来不能直接编译。** 那个文件 import 了 `upsertObservations` / `logFetch` /
> `ObservationInput`，而这三个符号已随采集层残留一并删除（§3.2.1）——所以这条路不是
> 「取回即可用」，得先把那三个补回去（SQL 在 git 历史里），或者只把它的数据生成部分
> 摘出来当一次性脚本跑。**这一条是删除死代码时新产生的代价，原记录写的时候还成立。**

为了对冲「不可再生」这个代价，测试从「测生成器」改成了**测产物**（见 §10.3）——生成器没了，
但界面对这份数据的每一条依赖仍然被钉住。

**不再有采集礼貌性问题**：应用**完全不联网**——渲染进程的 CSP 是 `connect-src 'none'`，
主进程也没有任何 `fetch`。原方案下每个用户的机器都会直连政府站点，因此有一整套限流
设计；那些约束随采集层一并移除了。

#### 3.2.1 网络采集设计（已删除）

> **§3.2.1 – §3.2.6 已删除。** 那是原网络采集方案的设计与实测记录：六个源（国家统计局、
> 人民银行、海关总署、财政部、外汇管理局、中国货币网）的实现方式、
> 首次启动的全量初始化与分级加载、采集礼貌性约束、降级链、采集器插件化，以及对真实
> 站点实测所得的 11 条结论。
>
> M1c 已整体移除采集层，抓取与解析代码不在工作区了。**写入路径也一并清掉了**——
> `db/queries.ts` 曾留着 `upsertObservations` / `logFetch` 两个函数，但采集层删除后它们
> 再没有任何调用方，属于纯粹的残留，已在 M2a 之后作为死代码删除；该文件现在只剩读路径。
> 原内容完整保存在 git 历史里
> （`git log -- docs/开发文档.md`）。其中关于 NBS 发布稿 HTML 结构、翻页回溯、
> 1—2 月合并列、WAF 行为的实测结论，在重新接入真实数据源时仍有参考价值，
> 届时从历史中取回。

### 3.3 数据库 Schema

SQLite，单文件。**关键设计：`period` 用于标识期间，`period_end` 用于排序与对齐。**

**表结构以代码为准**：v1 基线是 `src/main/db/schema.sql`（已冻结，只负责把空库建成 v1），
其后一切变更一律追加到 `src/main/db/migrate.ts` 的 migrations 数组。这里只记设计取舍，
不再誊写建表语句——誊写过的 schema 图迟早会跟代码分叉，本节原先那版就列了 8 张表，
而实际只有 4 张。

| 表 | 作用 |
|---|---|
| `observation` | 观测值。主键 `(indicator_id, period, revision)`，索引 `(indicator_id, period_end DESC)` |
| `fetch_log` | 数据来源健康度日志，驱动页脚的源状态显示。库里的那条是随文件带来的静态行 |
| `app_meta` | 键值对：`schema_version`（迁移用）、`last_fetch_at` |
| `release_index` | 发布页索引，v2 迁移建立（§3.3.1）。**当前是空表**——见 §3.3.1：它从建立起就没有过写入方 |

**指标元数据不在库里**，在 `src/shared/indicators.ts`——它是 `listIndicators` 的唯一事实源。

**Schema 设计要点：**

- `period` + `period_end` 分离 → 解决「月度、季度、年度指标混在一张对比图」的对齐问题。
- `revision` 进主键 → GDP 有初步核算/初步核实/最终核实三次修订，社融信贷月度修订。**修订历史必须保留**，不能直接覆盖。
- `status` 区分初值/修订值 → UI 上给修订值加标记。（M1a 起 `status` 共五态：`ok | prelim | revised | missing | merged`。UI 用**中文文字标签**呈现，映射表是 `Record<Observation['status'], string>` 的穷尽类型——漏一个编译期就报错，不会再出现直接印英文 `merged` 的情况。）
- `note` 字段承载口径说明 → 这是本产品相对同类工具的核心差异化，必须在 UI 显著位置展示。

#### 3.3.1 迁移机制（M1a 建立）

**`schema.sql` 就此冻结为 v1 基线**，文件头已写明；其后一切 schema 变更走 `src/main/db/migrate.ts`，`schema.sql` 只负责建空库。

```ts
const BASELINE_VERSION = 1        // schema.sql 建出来的版本
interface Migration { version: number; label: string; up: (db: Db) => void }
// 模块加载时校验 version 连续（必须是 BASELINE_VERSION+1+i），断了就直接抛
export const SCHEMA_VERSION = …   // 取最后一个迁移的 version
export function migrate(db: Db): { from: number; to: number; applied: string[] }
```

`migrate()` 读 `app_meta.schema_version`：

| 库中记录 | 行为 |
|---|---|
| 缺失 | 视为 v1（M0 装机的库就是这种），顺序补齐 |
| < `SCHEMA_VERSION` | 顺序补齐，**每个迁移单独一个事务** |
| = `SCHEMA_VERSION` | 什么都不做 |
| > `SCHEMA_VERSION` | **抛错拒绝打开**——绝不按旧 schema 去读写一个更新的库 |
| 非法值 | 抛错，**不静默当成 0 重来** |

⚠️ **建这个机制时踩到的坑（M0 遗留）：** `initDatabase()` 里有一句无条件的 `setMeta(handle, 'schema_version', '1')`，**每次启动都把版本号压回 1**，等价于没有版本记录。M0 期间它被 `CREATE TABLE IF NOT EXISTS` 完全掩盖，看不出任何症状；但只要写出第一个 `ALTER TABLE ADD COLUMN`，就会变成**每次启动都抛 `duplicate column name`**。这句话已删除。

v1 → v2 的内容是建 `release_index`（`url` 主键，另有 `source_id`、`title`、`published_at`、`page`、`fetched_at`）——原为 M1b 的发布索引准备，但 M1b 已暂缓、采集层又整体删除，**所以这张表从建立起就没有写入方，一直是 0 行**。它是迁移机制的第一个真实用例，本身留着无害。

**真机验证（2026-09-25）：** 拿 M0 装机现场的库（`schema_version=1`、20 条观测、无 `release_index`）启动，日志输出 `v1 → v2：v2 release_index`，重启后输出 `v2（已是最新，期望 v2）`，20 条观测原样保留。


### 3.4 数据口径陷阱（必须在实现中处理）

这一节是本项目最容易出错的地方，逐条对照实现。

| # | 陷阱 | 处理方式 |
|---|---|---|
| 1 | **1-2 月合并发布** | 统计局因春节错月，1 月和 2 月数据合并发布。因此**对这几个指标** `period='2026-01'` 不存在，这是**正常**的，不要报缺失。**实现：合并期间的 `period` 记为 `2026-02`（2 月是期间末月），`status` 记为 `'merged'`，UI 显示「1—2月合并」。** ⚠️ 原方案是「在 `note` 标注」，但 `note` 字段在 `indicator` 上、不在 `observation` 上，走 note 需要改表；改用 `status` 是更轻的路径。⚠️ **合并豁免必须按指标单独开关**，**只给工业增加值（同比 + 环比）、社零、固投、房地产开**：CPI / PPI / PMI / 失业率是**按月单独发布**的，库里各有 5 个 1 月观测值；通用豁免会让这 20 条观测凭空消失，且没有任何症状。 |
| 2 | **累计值 ≠ 单月值** | 固投、社零、财政收入在年内公布的是**累计同比**。`valueType` 必须准确，图表 Y 轴标题必须写明"累计同比"。 |
| 3 | **同比 / 环比 / 累计同比是三个指标** | 拆成独立的 `indicator` 记录（库里现成的例子是 `cn.ind_prod.yoy` 与 `cn.ind_prod.mom` 这一对），**绝不混画一张图**。 |
| 4 | **季调 vs 未季调** | GDP 当季**环比**是季调后的，当季同比不是。`seasonalAdj` 标记，UI 在图注写明。 |
| 5 | **基期轮换** | CPI 每 5 年轮换基期（2021–2025 用 2020 年基期）。跨基期的长序列严格来说不可直接比较，`note` 需说明。 |
| 6 | **数据修订** | 保留 `revision`，UI 在数据表里标注"已修订"，并用虚线或角标标出。 |
| 7 | **双单位口径** | 进出口同时有美元和人民币口径；M2 单位是"亿元"。`unit` 字段必须准确，禁止跨单位同图。 |
| 8 | **发布滞后** | CPI / PPI 次月 9 日、PMI 当月最后一天、GDP 季末次月 16 日（库内实测：2026Q2 → 2026-07-16）。同一「月份」的指标并非同时到齐，看板上的最新一期因此可能长短不一——这是发布节奏，不是数据缺失。 |
| 9 | **累计值换算** | 若需从累计同比推算单月同比，注意这是**近似计算**，且 1-2 月无法拆分。若做此换算，必须标注为"计算值"。 |
| 10 | **时区与日期** | 发布时刻用北京时间；`period_end` 用自然月末，不用"第几周"。 |

> ⚠️ **上表是「要求」，不全是「现状」——有两处是缺口，读的时候别当成已实现：**
>
> - 第 2 条的后半句「图表 Y 轴标题必须写明累计同比」：`lineOption.ts` 的 `yAxis` **没有 `name`**，
>   图上任何位置都没写「累计同比」。目前只有详情页的口径说明（`note`）和单位能传达这件事。
> - 第 6 条的后半句「用虚线或角标标出」修订值：**没有做**。图上只有 `merged` 有菱形点，
>   修订值（`status='revised'`）在图上没有任何标记，只有切到数据表才看得到「已修订」。
>
> 其余各条与实现一致。


### 3.5 更新调度

> **本节已删除。** 原内容是网络采集期的更新频率、修订窗口与调度策略（「日常只拉最近
> 3 期」、初始化与日常更新分开实现等）。应用当前不联网、也不更新数据——只有右上角的
> 「重置示例数据」按钮把 userData 那份覆盖回内置文件。需要时见 `git log -- docs/开发文档.md`。
